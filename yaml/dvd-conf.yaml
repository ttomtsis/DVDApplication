# ConfigMap for the MySQL Server and Router Pods
# as well as for the Spring Boot REST Server Pods
apiVersion: v1
kind: ConfigMap
metadata:
  name: dvd-config
data:
  mysql_database: "mydb"

  datasource_url: "jdbc:mysql://mycluster:3306/mydb"
  spring_profiles_active: "containerized"


